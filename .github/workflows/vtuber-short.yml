name: VTuber Auto-Short

# ──────────────────────────────────────────────────────────────────────────
# Triggers
# ──────────────────────────────────────────────────────────────────────────
on:
  # Run automatically every day at 12:00 UTC
  schedule:
    - cron: "0 12 * * *"

  # Trigger manually from the Actions tab with an optional topic hint
  workflow_dispatch:
    inputs:
      custom_topic:
        description: "Optional topic hint for the AI (leave blank for autonomous choice)"
        required: false
        default: ""

permissions:
  contents: read

jobs:
  generate-and-upload:
    name: Generate Short & Upload to YouTube
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Install fonts for FFmpeg subtitle rendering
      - name: Install fonts
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y fonts-liberation fonts-noto

      # Confirm FFmpeg is available (pre-installed on ubuntu-latest)
      - name: Verify FFmpeg
        run: ffmpeg -version | head -1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: scripts/requirements.txt

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Generate short video and upload to YouTube
        env:
          POLLINATIONS_API_KEY: ${{ secrets.POLLINATIONS_API_KEY }}
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
          CUSTOM_TOPIC: ${{ inputs.custom_topic }}
        run: python scripts/generate_short.py
